[build]
  functions = "netlify/functions"

[functions."scheduled-post"]
  # Runs once a day. Cron is UTC. 07:00 UTC = 07:00/08:00 UK time depending on DST.
  # Change this once we agree on the exact time you want the daily run to fire.
  schedule = "0 7 * * *"
